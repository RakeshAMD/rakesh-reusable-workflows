name: Create and Publish Docker Image

on:
  push:
  workflow_call:


jobs:
  build:
    runs-on: ubuntu-latest

    steps:      
      
      - name: Set the tag
        run: |
            echo "Hi"
            
  do-something-else:
    runs-on: self-hosted

    steps:
    - run: echo "Hello"
